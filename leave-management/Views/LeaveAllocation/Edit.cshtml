@model leave_management.Models.EditLeaveAllocationViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Editing @Model.LeaveType.Name Allocation for @Model.Employee.LastName, @Model.Employee.FirstName  </h1>


<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="EmployeeId" />
            <div class="form-group">
                <label asp-for="NumberOfDays" class="control-label"></label>
                <input asp-for="NumberOfDays" class="form-control" />
                <span asp-validation-for="NumberOfDays" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" style="margin-right:5px" class="btn btn-primary" /> <a class="btn btn-outline-secondary" asp-action="Details" asp-route-id="@Model.EmployeeId">Back to Details</a>
            </div>
        </form>
    </div>
</div>
